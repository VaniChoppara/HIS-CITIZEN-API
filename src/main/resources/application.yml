spring:
  application:
    name: CITIZEN-API
  config:
    import: optional:configserver:http://localhost:2222
    
  mail:
    host: smtp.gmail.com
    password: <gmail generated pwd>
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: <your gmail id>
  

